export class PizzaModel {
    constructor(public id: string, public topping: string, public bread: string, public cheese: string, public price: number, public image: string, public type: string) { }
}